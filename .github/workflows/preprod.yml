name: Release

on:
  push:
    branches:
      - 'preprod'
jobs:
  shared:
    uses: BlusparkTeam/github-workflows/.github/workflows/build-deploy-superset.yaml@main
    secrets: inherit
    with:
      environment: preprod
